project_name: "TradeLab"
version: "1.0.0"

data:
  raw_path: "data/raw"
  processed_path: "data/processed"
  start_date: "2020-01-01"
  end_date: "2023-12-31"

strategies:
  momentum:
    fast_window: 50
    slow_window: 200
  mean_reversion:
    window: 20
    std_dev: 2

backtest:
  initial_capital: 100000.0
  commission: 0.001  # 0.1% per trade

llm:
  provider: "mock"  # or 'openai' if implemented
